{
  "name": "bathily-convoyage",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "postbuild": "node scripts/copy-social-media-assets.mjs",
    "preview": "vite preview",
    "postinstall": "node scripts/generate-config.cjs",
    "test": "npx playwright test",
    "test:ui": "npx playwright test --ui",
    "test:report": "npx playwright show-report",
    "test:buffer": "node scripts/test-buffer-social.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.43.4",
    "dotenv": "^16.4.5",
    "googleapis": "^140.0.0",
    "puppeteer": "^25.3.0",
    "stripe": "^15.12.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "vite": "^5.2.0"
  }
}
